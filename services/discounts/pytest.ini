[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (API, database)
    flaky_training: Intentionally flaky tests for educational purposes

addopts =
    -v
    --strict-markers
